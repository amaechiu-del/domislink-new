name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger Render Deploy Hook
        run: curl -X POST https://api.render.com/deploy/srv-d47h2g2li9vc738nkt20?key=nL0mNks1cKE
